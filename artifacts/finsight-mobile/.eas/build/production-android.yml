build:
  name: Production Android build
  steps:
    - eas/checkout
    - run:
        name: Install dependencies
        command: pnpm install --no-frozen-lockfile
    - eas/prebuild
    - eas/run_gradle
