build:
  name: Production Android build
  steps:
    - eas/checkout
    - run:
        name: Upgrade pnpm to compatible version
        command: npm install -g pnpm@10.26.1
    - run:
        name: Install dependencies
        command: pnpm install --no-frozen-lockfile
    - eas/prebuild
    - eas/run_gradle
